# Descripción 
Este proyecto consiste en la implementación de un servidor web básico en Java, desarrollado utilizando la API de sockets TCP y el modelo de programación multi-hilos. El servidor escucha conexiones entrantes en un puerto configurable mayor a 1024 y opera de forma continua, atendiendo múltiples solicitudes HTTP de manera concurrente.
